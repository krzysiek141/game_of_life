- [x] create simple python function that accepts a initial board for game of life as a matrix and generates subsequent future generations in a number provided as an argument
- [x] build a simple flask rest api and add an endpoint to generate new generations
- [x] add a simple frontend with grid made of simple diffs
- [ ] fill in the gitignore and publish the repository, commit the initial changes
- [ ] dockerize this app
- [ ] add simple authentication layer to the api
- [ ] add possibility to use grpc


I would like to build a very simple frontend for my app so that I could view the generations returned by my api. I just need a simple html file with basic styling and two buttons to navigate beetween generations. I also need a separate fixed board to input a initial board and send it to the rest server